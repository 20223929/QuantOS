name: Docker Build

on:
  push:
    branches:
      - origin/master
  pull_request:
    branches:
      - origin/master

jobs:
  docker-build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Docker build
        run: |
          docker build -t quantos-next .
